services:
  hackaton-client:
    container_name: hackaton-client
    build: ./client
    ports:
      - "4200:4200"
      - "9876:9876"
    volumes:
      - ./client:/usr/src/app
    stdin_open: true
    tty: true
    command: /bin/sh -c "npm i && npm run build"